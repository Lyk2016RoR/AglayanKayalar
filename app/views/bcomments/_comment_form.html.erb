<% if current_user.present? %>
<%= form_for [@book, @book.bcomments.build] do |f| %>
	<%= f.label :Comment%><br>
	<%= f.text_area :body, placeholder: 'comment', rows: 10 %><br><br>
	<%= f.submit 'Gonder'%>
<% end %>
<%end%>